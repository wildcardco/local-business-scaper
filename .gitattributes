# Ensure database files are never committed even if somehow added
*.db filter=lfs diff=lfs merge=lfs -text
*.db-shm binary
*.db-wal binary

# Never commit environment files
.env filter=lfs diff=lfs merge=lfs -text
